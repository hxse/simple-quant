{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/widget.jsx --minify --format=esm --bundle --outdir=src/anywidget_demo/static"
	},
	"dependencies": {
		"@anywidget/react": "^0.0.4",
		"react": "^18.2.0",
		"react-dom": "^18.2.0",
		"uplot": "^1.6.30",
		"uplot-react": "^1.1.5"
	},
	"devDependencies": {
		"esbuild": "^0.20.0"
	}
}
